const {filteredResponseValidatorSchema} = require('./filteredResponsesSchema');
const {filloutApiSchema} = require('./filloutApiSchema');

module.exports = {
    filteredResponseValidatorSchema,
    filloutApiSchema
}